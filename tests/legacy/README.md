# Archived Standalone Tests

These HTML files document checks for the former standalone application. They are retained for reference only: their relative paths and assertions target the old project structure and question bank, and they are not a supported test suite for the account-based application.

Use `npm test` and `npm run test:browser` from the project root instead.
